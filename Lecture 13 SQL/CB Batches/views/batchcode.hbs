<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Branch Code</title>
</head>
<body>
    <h1>Generate Batch Code</h1>
    <form action='/batchcode' method='POST'>
        <b>Center</b>
        <select name='center'>
            {{#each centers as |c|}}
                <option value='{{c.id}}'>{{c.name}}</option>
            {{/each}}
        </select><br>
        <b>Season</b>
        <select name='season'>
            {{#each seasons as |c|}}
                <option value='{{c.id}}'>{{c.name}}</option>
            {{/each}}
        </select><br>

        <b>Courses</b>
        <select name='course'>
            {{#each courses as |c|}}
                <option value='{{c.id}}'>{{c.name}}</option>
            {{/each}}
        </select><br>

        <b>Years</b>
        <select  name='year'>
            {{#each years as |y|}}
                <option value='{{y}}'>{{y}}</option>
            {{/each}}
        </select><br>

        <b>Batch Number</b>
        <input name='batchno' type='number' min='1' max='5'><br>
        <b>Start Date</b>
        <input name='stdate' type='date'><br>
        <b>End Date</b>
        <input name='endate' type='date'><br>
        <button type='submit'>SUBMIT</button>


    </form>
</body>
</html>